<div class="container">
    <div class="row">
        <div class="span3">
            <h4>Cubes</h4>
            <ul class="unstyled">
                {{#each model}}
                    {{#linkTo 'cube' this}}<li>
                        {{cube_number}} <small class="text-success">{{image_type}}</small> <small class="muted"> {{date created}}</small>
                    </li>{{/linkTo}}
                {{/each}}
            </ul>
        </div>
        <div class="span8">
            {{outlet}}
        </div>
    </div>
</div>